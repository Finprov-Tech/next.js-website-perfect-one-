# Server-only — the browser never talks to Django directly, only this
# app's own API routes do (see src/lib/api.ts), so this never needs to be
# NEXT_PUBLIC_ and Django's CORS settings don't need to know about this app.
DJANGO_API_URL=http://127.0.0.1:8000
